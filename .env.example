# Flac Guard — Environment Variables

# JWT secret for dashboard authentication
JWT_SECRET=change-me-to-a-random-secret-key

# Database (used by docker-compose)
POSTGRES_USER=flac_guard
POSTGRES_PASSWORD=flac_guard
POSTGRES_DB=flac_guard

# HappyDo Pulse integration (for PDV sync)
PULSE_API_URL=https://happydopulse-production.up.railway.app/api
PULSE_EMAIL=
PULSE_PASSWORD=

# Deploy webhook secret (auto-generated by setup.sh)
WEBHOOK_SECRET=

# S3 Object Storage (optional — recordings stay local if not configured)
# Works with AWS S3, Contabo Object Storage, MinIO, DigitalOcean Spaces, etc.
S3_ENDPOINT=
S3_BUCKET=flac-guard-recordings
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1
S3_RECORDINGS_PREFIX=recordings
S3_FACES_PREFIX=faces
S3_WATCHLIST_PREFIX=watchlist
